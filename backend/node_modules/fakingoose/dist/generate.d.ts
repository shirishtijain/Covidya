import { Schema, Document } from 'mongoose';
import { FactoryOptions, GlobalOptions, MockerFieldOption } from './types';
export declare type GeneratorOptions = {
    options: MockerFieldOption;
    staticFields: Record<string, unknown>;
    globalOptions: GlobalOptions;
};
declare type GenerateOptions<T extends Document> = {
    options: FactoryOptions;
    staticFields: Record<string, unknown>;
    globalOptions: GlobalOptions;
};
export declare function generate<T extends Document>(schema: Schema<T>, opts: GenerateOptions<T>): T;
export {};
